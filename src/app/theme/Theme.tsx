import { createTheme } from "@mui/material/styles";

const themeApp = createTheme({
  palette: {
    primary: {
      main: "#1187f2",
    },
    secondary: {
      main: "#419e6a",
    },
  },
});

export default themeApp;
